import 'package:flutter/material.dart';

class CreateLogScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Create Log"),
      ),
      body: Center(
        child: Text('This is the Create Log screen'),
      ),
    );
  }
}
